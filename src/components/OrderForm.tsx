import { FormEvent, useState } from 'react';
import { ArrowRight, MessageCircle, Send } from 'lucide-react';
import { PRINT_SIZE_PRESETS, TSHIRT_COLORS, WHATSAPP_CONFIG } from '../data/content';

export default function OrderForm() {
  const [technique, setTechnique] = useState('DTF Digital Full Color');
  const [size, setSize] = useState(PRINT_SIZE_PRESETS[1].name);
  const [quantity, setQuantity] = useState(12);
  const [submitted, setSubmitted] = useState(false);
  const selectedSize = PRINT_SIZE_PRESETS.find((item) => item.name === size) || PRINT_SIZE_PRESETS[1];
  const unitPrice = technique.startsWith('DTF') ? selectedSize.priceDtf : selectedSize.priceManual;
  const handleSubmit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const get = (key: string) => String(data.get(key) || '-'); const message = `Halo Planet Store, saya ingin konsultasi order sablon kaos custom.\n\nNama: ${get('name')}\nNomor WhatsApp: ${get('phone')}\nJenis sablon: ${get('technique')}\nBahan: ${get('fabric')}\nWarna: ${get('color')}\nUkuran sablon: ${get('size')}\nJumlah: ${get('quantity')} pcs\nEstimasi: Rp ${(unitPrice * quantity).toLocaleString('id-ID')}\nTarget selesai: ${get('deadline')}\nCatatan: ${get('notes')}\n\nMohon konfirmasi harga finalnya. Terima kasih.`; setSubmitted(true); window.open(`https://wa.me/${WHATSAPP_CONFIG.phoneNumber}?text=${encodeURIComponent(message)}`, '_blank', 'noopener,noreferrer'); };
  const field = 'mt-2 min-h-11 w-full rounded-xl border border-neutral-300 bg-white px-3 text-sm font-normal outline-none focus:border-neutral-950 focus:ring-2 focus:ring-neutral-950/20';
  return <section id="order-form" className="bg-neutral-100 py-14 text-neutral-950 md:py-20" aria-labelledby="order-form-title"><div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8"><div className="mb-8 max-w-2xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-neutral-500">Mulai Pesanan</p><h2 id="order-form-title" className="mt-3 text-3xl font-black tracking-tight sm:text-5xl">Isi spesifikasi kaos Anda.</h2><p className="mt-4 text-sm leading-6 text-neutral-600">Lengkapi kebutuhan produksi dan dapatkan estimasi biaya langsung di form yang sama.</p></div><form onSubmit={handleSubmit} className="rounded-3xl border border-neutral-200 bg-white p-5 shadow-sm sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-bold">Nama lengkap<input required name="name" autoComplete="name" className={field} placeholder="Nama Anda" /></label><label className="text-sm font-bold">Nomor WhatsApp<input required name="phone" inputMode="tel" autoComplete="tel" className={field} placeholder="08xxxxxxxxxx" /></label><label className="text-sm font-bold">Jenis sablon<select name="technique" value={technique} onChange={(e) => setTechnique(e.target.value)} className={field}><option>DTF Digital Full Color</option><option>Sablon Manual Screenprint</option></select></label><label className="text-sm font-bold">Bahan kaos<select name="fabric" className={field}><option>Cotton Combed 30s</option><option>Cotton Combed 24s</option><option>Heavyweight Cotton 20s</option></select></label><label className="text-sm font-bold">Warna kaos<select name="color" className={field}>{TSHIRT_COLORS.map((color) => <option key={color.id}>{color.name}</option>)}</select></label><label className="text-sm font-bold">Ukuran sablon<select name="size" value={size} onChange={(e) => setSize(e.target.value)} className={field}>{PRINT_SIZE_PRESETS.map((item) => <option key={item.id}>{item.name}</option>)}</select></label><label className="text-sm font-bold">Jumlah kaos<input required name="quantity" type="number" min="1" value={quantity} onChange={(e) => setQuantity(Math.max(1, Number(e.target.value)))} className={field} /></label><label className="text-sm font-bold">Target selesai<input name="deadline" type="date" className={field} /></label><label className="text-sm font-bold sm:col-span-2">Catatan tambahan<textarea name="notes" rows={3} className="mt-2 w-full rounded-xl border border-neutral-300 px-3 py-3 text-sm font-normal outline-none focus:border-neutral-950 focus:ring-2 focus:ring-neutral-950/20" placeholder="Kebutuhan event, desain depan-belakang, atau pertanyaan Anda." /></label></div><div className="mt-6 rounded-2xl bg-neutral-950 p-4 text-white sm:flex sm:items-center sm:justify-between"><div><p className="text-xs text-neutral-400">Estimasi biaya cetak</p><p className="mt-1 text-2xl font-black">Rp {(unitPrice * quantity).toLocaleString('id-ID')} <span className="text-xs font-normal text-neutral-400">({quantity} pcs × Rp {unitPrice.toLocaleString('id-ID')})</span></p></div><p className="mt-2 text-xs text-neutral-300 sm:mt-0">Harga final dikonfirmasi setelah cek desain.</p></div><div className="mt-6 flex flex-col items-start gap-3 sm:flex-row sm:items-center"><button type="submit" className="inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-neutral-950 px-6 text-sm font-bold text-white transition hover:bg-neutral-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950 sm:w-auto"><Send className="h-4 w-4" aria-hidden="true" /> Kirim ke WhatsApp <ArrowRight className="h-4 w-4" aria-hidden="true" /></button><span className="inline-flex items-center gap-2 text-xs text-neutral-500"><MessageCircle className="h-4 w-4 text-emerald-600" aria-hidden="true" /> {submitted ? 'Detail siap dikirim.' : 'Konsultasi dan estimasi gratis.'}</span></div></form></div></section>;
}
