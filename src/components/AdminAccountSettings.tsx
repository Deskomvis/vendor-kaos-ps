import { FormEvent, useState } from 'react';
import { KeyRound, Mail, Save } from 'lucide-react';
import { supabase } from '../lib/supabase';

export default function AdminAccountSettings() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const update = async (event: FormEvent) => { event.preventDefault(); setMessage(''); setError(''); if (!supabase) return; const payload = email || password ? { ...(email ? { email } : {}), ...(password ? { password } : {}) } : {}; const { error: updateError } = await supabase.auth.updateUser(payload); if (updateError) setError(updateError.message); else { setMessage('Pengaturan akun berhasil diperbarui.'); setPassword(''); } }; return <section className="rounded-3xl border border-neutral-200 bg-white p-5 shadow-sm sm:p-8"><h1 className="text-2xl font-black">Pengaturan admin</h1><p className="mt-2 text-sm text-neutral-600">Ganti email login atau password akun admin.</p><form onSubmit={update} className="mt-6 max-w-xl space-y-5"><label className="block text-sm font-bold"><span className="inline-flex items-center gap-2"><Mail className="h-4 w-4" /> Email baru</span><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="Biarkan kosong jika tidak diubah" className="mt-2 w-full rounded-xl border border-neutral-300 px-3 py-3 text-sm" /></label><label className="block text-sm font-bold"><span className="inline-flex items-center gap-2"><KeyRound className="h-4 w-4" /> Password baru</span><input type="password" minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Minimal 8 karakter" className="mt-2 w-full rounded-xl border border-neutral-300 px-3 py-3 text-sm" /></label>{error && <p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}{message && <p className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{message}</p>}<button className="inline-flex items-center gap-2 rounded-xl bg-neutral-950 px-5 py-3 text-sm font-bold text-white"><Save className="h-4 w-4" /> Simpan pengaturan</button></form></section>; }
