import { useState } from 'react';
import { Eye, KeyRound, LogOut, Menu, Tags, X } from 'lucide-react';
import AdminVisualEditor from './AdminVisualEditor'; import AdminPricingEditor from './AdminPricingEditor'; import AdminAccountSettings from './AdminAccountSettings'; import WhatsAppRotatorEditor from './WhatsAppRotatorEditor'; import { supabase } from '../lib/supabase';
export default function AdminDashboardShell() { const [active, setActive] = useState<'preview'|'pricing'|'rotator'|'account'>('preview'); const [open, setOpen] = useState(false); const items = [{ id: 'preview', label: 'Preview Editor', icon: Eye }, { id: 'pricing', label: 'Edit Harga', icon: Tags }, { id: 'rotator', label: 'Rotator WhatsApp', icon: Menu }, { id: 'account', label: 'Pengaturan Admin', icon: KeyRound }] as const; const content = active === 'preview' ? <AdminVisualEditor /> : active === 'pricing' ? <AdminPricingEditor /> : active === 'rotator' ? <WhatsAppRotatorEditor /> : <AdminAccountSettings />; return <div className="min-h-screen bg-neutral-100"><aside className={`fixed inset-y-0 left-0 z-50 w-72 border-r border-neutral-200 bg-white p-5 transition-transform lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center justify-between"><p className="font-black">Gudang Planet CMS</p><button className="lg:hidden" onClick={() => setOpen(false)}><X className="h-5 w-5" /></button></div><nav className="mt-8 space-y-2">{items.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => { setActive(id); setOpen(false); }} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold ${active === id ? 'bg-neutral-950 text-white' : 'hover:bg-neutral-100'}`}><Icon className="h-4 w-4" /> {label}</button>)}<button onClick={() => supabase?.auth.signOut()} className="mt-8 flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold text-red-700 hover:bg-red-50"><LogOut className="h-4 w-4" /> Logout</button></nav></aside><button onClick={() => setOpen(true)} className="fixed left-4 top-4 z-40 rounded-xl bg-white p-3 shadow lg:hidden"><Menu className="h-5 w-5" /></button><div className="lg:pl-72">{content}</div></div>; }
